;;
;; 色の設定（themeフレームワークを利用しない場合の設定）
;;

;; FrameとWindowの色を設定する
;(set-face-foreground 'default "green")
;(set-face-background 'default "black")

;; アクティブなmodelineの色を設定する
;(set-face-foreground 'mode-line "White")
;(set-face-background 'mode-line "Navy")

;; インアクティブなmodelineの色を設定する
;(set-face-foreground 'mode-line-inactive "White")
;(set-face-background 'mode-line-inactive "BlueViolet")

;; 対応する括弧をハイライト表示
;(setq show-paren-mode t)

;; リージョンに色をつける
;(setq transient-mark-mode t)

;; リージョンの色を設定する
;(set-face-background 'region "Maroon")

;; カーソルの色を設定する
;(add-to-list 'default-frame-alist '(cursor-color . "White"))
;(mac-set-input-method-parameter 'japanese 'cursor-color "moccasin")
;(mac-set-input-method-parameter 'roman 'cursor-color "snow")

;; マウスポインタの色を設定する
;(add-to-list 'default-frame-alist '(mouse-color . "SlateBlue2"))

