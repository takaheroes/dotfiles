;;
;; 色の設定（themeフレームワークを利用）
;;

;; themeファイルの格納場所を設定する
;(setq custom-theme-directory "~/.emacs.d/elisp/theme")

;; molokaiのカラーテーマを利用する
;(load-theme 'molokai t)

;; solarizedのカラーテーマを利用する
(load-theme 'sanityinc-solarized-dark t)

